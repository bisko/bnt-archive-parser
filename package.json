{
  "name": "bnt-archive-parser",
  "version": "1.0.0",
  "main": "src/start.js",
  "scripts": {
    "start": "yarn build && node build/start.js",
    "build": "babel src -d build"
  },
  "license": "GPL-2.0-or-later",
  "dependencies": {
    "argparse": "^1.0.10",
    "cheerio": "^1.0.0-rc.3",
    "request": "^2.88.0",
    "request-promise-native": "^1.0.7",
    "yargs": "^14.2.0"
  },
  "devDependencies": {
    "@babel/cli": "^7.6.4",
    "@babel/core": "^7.6.4",
    "@babel/preset-env": "^7.6.3"
  }
}
